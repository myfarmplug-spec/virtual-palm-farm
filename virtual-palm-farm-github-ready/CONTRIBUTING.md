# Contributing

Thanks for contributing! Basic workflow:

1. Fork the repo
2. Create a branch: `git checkout -b feature/my-change`
3. Make changes & run `npm install` and `npm run dev` to test
4. Commit and push, then open a Pull Request
